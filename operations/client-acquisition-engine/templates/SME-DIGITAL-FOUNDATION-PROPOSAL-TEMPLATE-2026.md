# <Outcome-Focused Proposal Title>

Prepared for: `<company>`  
Prepared by: BridgeWorks  
Contact: Emmanuel Ehigbai, office@bridgeworks.agency  
Date: `<YYYY-MM-DD>`  
Valid until: `<YYYY-MM-DD>`  
Reference: `<proposal-reference>`

## Executive Summary

`<Company>` has a strong `<business capability or proof>`, but the current `<website, visibility, trust, or enquiry system>` makes it harder for `<target buyer>` to `<desired action>`.

The confirmed priority is `<problem in the prospect's words>`. It currently creates `<business consequence>`. BridgeWorks recommends a focused `<offer name>` to produce `<defined outcome>` without expanding into unrelated work.

## What We Observed

1. `<Observed fact and source>`
2. `<Observed fact and source>`
3. `<Observed fact and source>`

These observations were confirmed on `<date>`. They are the basis of this scope.

## Recommended Outcome

By the end of the project, `<company>` should have:

- `<Buyer-facing or operational outcome>`
- `<Buyer-facing or operational outcome>`
- `<Measurable implementation outcome>`

BridgeWorks commits to the work and agreed measurements, not to guaranteed rankings, AI citations, lead volume, or revenue.

## Scope

### Included

- `<Deliverable>`
- `<Deliverable>`
- `<Deliverable>`
- `<Testing and handover deliverable>`

### Deliberately Excluded

- `<Exclusion>`
- `<Exclusion>`
- `<Third-party or client-owned dependency>`

Anything outside this scope requires a written change decision before work begins.

## Delivery Approach

### 1. Confirm

Validate access, baseline evidence, final copy, and success measures.

### 2. Build

Implement the agreed trust, visibility, website, or enquiry-path changes.

### 3. Verify

Test the live buyer journey, technical requirements, and agreed measurements.

### 4. Handover

Deliver access, documentation, evidence, and the agreed support window.

## Relevant BridgeWorks Proof

CEEFM is a Budapest facility-management company with substantial operating experience but previously weak digital discoverability.

During the March-to-June 2026 engagement, BridgeWorks connected website infrastructure, structured data, sitemap and crawler access, clearer service content, reporting, and outbound preparation. BridgeWorks' verified GEO and AI-visibility diagnostic moved from 16/100 at baseline to 77/100 at the engagement-close audit.

The relevant lesson for `<company>` is `<specific connection between CEEFM's problem and this scope>`.

This result is evidence of the CEEFM engagement. It does not guarantee the same score, ranking, citation, lead volume, or commercial result for another company.

Do not add Oliviks proof until its completion checklist is met and the resulting asset is approved.

## Success Measures

| Measure | Baseline | Target or completion evidence | Source |
|---|---|---|---|
| `<measure>` | `<value>` | `<target or pass condition>` | `<source>` |
| `<measure>` | `<value>` | `<target or pass condition>` | `<source>` |
| `<measure>` | `<value>` | `<target or pass condition>` | `<source>` |

Success measures must be observable within the project. Do not use unmeasured revenue or ranking promises.

## Timeline

Estimated duration: `<duration>` from confirmed start, access, and required client materials.

| Stage | Timing | Client dependency |
|---|---|---|
| Confirm | `<timing>` | `<dependency>` |
| Build | `<timing>` | `<dependency>` |
| Verify | `<timing>` | `<dependency>` |
| Handover | `<timing>` | `<dependency>` |

Delays in access, approval, content, or third-party platforms may move the delivery dates.

## Investment

| Item | Investment | Notes |
|---|---:|---|
| `<primary scope>` | `<currency and amount>` | `<one-time or recurring>` |
| `<optional item>` | `<currency and amount>` | Optional |
| **Total** | **<currency and amount>** | `<tax treatment>` |

Payment schedule:

- `<amount or percentage>` on acceptance.
- `<amount or percentage>` at `<milestone>`.

The final proposal must use one confirmed invoice currency and the correct current tax treatment.

## Client Responsibilities

- Provide required access and accurate business information.
- Review decisions within `<time window>`.
- Confirm legal, regulatory, and brand-sensitive content.
- Assign one decision-maker for scope and approval.

## Assumptions and Limitations

- Third-party platforms control their own approvals, rankings, indexing, citations, and availability.
- BridgeWorks cannot guarantee search rankings, AI citations, enquiry volume, or revenue.
- Any baseline score is method-specific and must name its date and source.
- Existing technical debt outside the confirmed scope may require a written change decision.

## Next Steps

1. Confirm the scope and investment.
2. Resolve any open assumptions.
3. Approve the proposal in writing.
4. Complete the agreed initial payment.
5. Begin after access and start conditions are confirmed.

BridgeWorks  
office@bridgeworks.agency  
https://bridgeworks.agency

---

## Internal Pre-Send Checklist

Remove this section before sending.

- Prospect problem is stated in their words.
- Scope maps to one primary offer.
- Every observation was rechecked live.
- CEEFM claim is 16/100 to 77/100, March to June 2026.
- No Oliviks proof appears before completion and approval.
- No rankings, citations, lead volume, or revenue are guaranteed.
- Exclusions and dependencies are explicit.
- Currency, VAT treatment, payment schedule, and validity date are confirmed.
- Proposal reference and contact details are correct.
- Emmanuel reviewed the full proposal.
- Exported PDF was visually checked.
- Sending approval is recorded.
