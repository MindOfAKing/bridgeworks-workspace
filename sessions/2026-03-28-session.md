# Session — 2026-03-28

## What was worked on
Stripe payment flow for BridgeWorks — setup fee and monthly retainer.

## Files created or changed
- `src/lib/stripe.ts` — Stripe client (v21, API version 2026-03-25.dahlia)
- `src/lib/email.ts` — nodemailer/Gmail SMTP email confirmation sender
- `src/app/api/checkout/route.ts` — POST endpoint that creates Stripe Checkout sessions
- `src/app/api/webhook/route.ts` — Stripe webhook handler, sends email on payment, TODO Szamlazz
- `src/components/pay/PaymentOptions.tsx` — client component: product selector, currency toggle, auto-charge option
- `src/app/[locale]/pay/page.tsx` — payment page at /en/pay (all 4 locales)
- `src/app/[locale]/pay/success/page.tsx` — confirmation page after Stripe redirects back
- `.env.local` — added STRIPE_WEBHOOK_SECRET (placeholder), NEXT_PUBLIC_BASE_URL, SMTP vars

## Env vars added to .env.local (NOT committed)
- `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
- `STRIPE_SECRET_KEY`
- `SMTP_USER` / `SMTP_PASS` / `SMTP_FROM`
- `STRIPE_WEBHOOK_SECRET` — currently placeholder, needs real value (see below)
- `NEXT_PUBLIC_BASE_URL`

## Decisions made
- EUR and HUF supported. NGN deferred (Stripe account may not support it for Hungarian business).
- HUF uses hardcoded rates: setup 420,000 Ft / retainer 160,000 Ft. Update in `src/app/api/checkout/route.ts` PRICES constant.
- Retainer can be one-time or subscription (auto-charge toggle).
- Szamlazz integration left as TODO in webhook — find API key in Szamlazz.hu settings.
- Currently uses TEST keys (pk_test / sk_test). Swap for live keys before real clients pay.

## What is incomplete
1. **Webhook secret** — `STRIPE_WEBHOOK_SECRET` is a placeholder.
   - Go to Stripe Dashboard > Developers > Webhooks > Add endpoint
   - URL: `https://bridgeworks.agency/api/webhook`
   - Event: `checkout.session.completed`
   - Copy the signing secret and update `.env.local` + Vercel env vars
2. **Vercel env vars** — add all 7 env vars from `.env.local` to Vercel dashboard
3. **Szamlazz invoice** — TODO comment in webhook route, needs API key
4. **Live keys** — swap test keys for live when ready
5. **NGN currency** — not implemented, deferred

## Next recommended action
1. Add env vars to Vercel (Project Settings > Environment Variables)
2. Set up webhook in Stripe dashboard and get real STRIPE_WEBHOOK_SECRET
3. Test the full flow in test mode at bridgeworks.agency/en/pay
