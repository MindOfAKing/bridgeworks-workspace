# Session 2026-08-11 — Internal GTM package merged and made runnable

## What was worked on

Executed the `bridgeworks-internal-gtm-v1` implementation directive from Downloads
into `operations/internal-gtm/`.

## Directive steps

| Step | State |
|---|---|
| 1. Add the package | Done. 16 roles, routing policy, 3 schemas, role briefs. |
| 2. Loader/adapter for the canonical registry | Done. `scripts/capability_loader.py`. |
| 3. Tests for no dangling capability references | Done. 11 tests. All 18 referenced capabilities resolve against the live 74-skill registry. Zero dangling. |
| 4. GEO vertical slice | Done. `scripts/run_geo_slice.py`, runs end to end to `awaiting_approval`. |
| 5. Market-discovery vertical slice | Done. `scripts/run_market_discovery.py`, produces a ranked queue JSON and CSV. |
| 6. Deterministic dedupe and scoring | Done. `scripts/gtm_core.py`. Order-independent, proved by test. |
| 7. ODS/Qwen bounded to validated preprocessing | Encoded in `routing-policy.yaml`, surfaced on every capability call request. |
| 8. Claude/Codex for contradictions and commercial reasoning | Same. Contradiction detection is deterministic, resolution is routed out. |
| 9. Approval and no-send rules preserved | Done. `advance()` raises on `sent` and later. Packets carry `no_send`, `external_action_authorized: false` and a SHA-256 digest. |
| 10. GTM migration/decommission receipts | Preserve step done, read-only. `receipts/2026-08-11-preserve-inventory.md`. |

## Files created

- `operations/internal-gtm/` — README, directive, migration manifest, `role-registry.yaml`, `routing-policy.yaml`, `service-routes.yaml`
- `roles/` — 16 role briefs
- `schemas/` — gtm-state, capability-contract, geo-diagnostic
- `scripts/` — `capability_loader.py`, `gtm_core.py`, `run_geo_slice.py`, `run_market_discovery.py`
- `tests/` — `test_architecture.py`, `test_capability_references.py`, `test_gtm_core.py`
- `examples/` — three synthetic fixtures
- `runs/` — fixture run records
- `receipts/` — preserve inventory

Changed: `CLAUDE.md`, one line under What Lives Here.

## Verification

- 49 tests pass in `operations/internal-gtm/tests`
- 10 existing tests still pass in `skill-governance/tests`
- `capability_loader.py` reports 74 capabilities, 16 roles, 18 references, 0 dangling
- Both slice CLIs run on the fixtures and write records

## Decisions made

- No second capability registry. The loader reads `skill-governance/capability-registry.yaml` and a test fails if a second one appears under internal-gtm.
- The five service routes in `service-routes.yaml` were verified against the deployed site on 2026-08-11, not taken from a local file.
- No price anywhere in the engine. The 2026-06 lead-engine-v1 offer ladder and the 2026-07-14 single-offer decision conflict, so price stays `confirmed_at_booking`.
- Added one routing task, `diagnostic_capability_run` to `claude_code`. The shipped policy had no route for running geo-audit or client-audit. Marked inline with its reason.
- Tier names reuse HOT/WARM/COLD from the existing lead-scoring rubric.

## Incomplete

- `operations/client-acquisition-engine/` overlaps this package and is live: 7,911 files, a scheduled 09:10 weekday run, state modified today. It is not in the shipped manifest. Three options are written up in `MIGRATION-DECOMMISSION.md`. No decision made.
- No migration executed. Preserve inventory only.
- Hermes GTM jobs and browser-local Ops state are outside this repository and were not inventoried.
- Nothing committed. AGENTS.md requires approval to commit or push.
- Emmanuel OS Command Center not updated. `update_emmanuel_os.py` needs correcting for this session first.

## Next recommended action

Decide the `client-acquisition-engine` question. Everything downstream of it,
including whether the migration runs at all, is blocked on that one call.

---

# Phase 2 (same day) — Acquisition engine absorption

Decision from Emmanuel: `operations/client-acquisition-engine/` is the active GTM
migration source. Keep, absorb, retire only duplicated control and state surfaces
after verified migration. No bulk move.

## Deliverables

| # | Deliverable | Where |
|---|---|---|
| 1 | Functional inventory, 34 components | `migration/acquisition-engine-map.json` + `acquisition-engine-inventory.md` |
| 2 | Source-to-target map across 7 areas | same map, `target` field per component |
| 3 | Superseded logic, 6 marked | `migration/superseded-logic.md` + 5 in-file markers |
| 4 | 09:10 redesign | `scripts/daily_objective.py`, `adapters/engine_snapshot.py`, `migration/daily-objective-redesign.md` |
| 5 | Real-prospect comparison, Premier FM | `migration/phase-2-test-results.md`, `runs/geo/premier-fm-2026-08-11.json` |
| 6 | GEO test, GBS Africa | same, `runs/geo/gbs-africa-2026-08-11.json` |
| 7 | Non-GEO routing test | same |
| 8 | Phase 3 plan, 10 steps | `migration/PHASE-3-PLAN.md` |

## Results

- 7,911 files in the engine. 7,717 are one xlsx build directory. The real engine is
  194 files across 34 components.
- Premier FM: 94/100 HOT, route `digital-platforms-brand`, diagnostic `client-audit`.
  Not GEO.
- GBS Africa: geo-audit run with all five subagents, composite 45/100. 77/100 HOT,
  route `content-visibility-demand`, diagnostic `geo-audit`.
- 09:10 replacement against real state chose `recover_overdue_research`, not new
  supply. 9 ready-and-uncontacted prospects, 2 overdue batches, 20 approvals waiting.

## Defects found and fixed

- Contradiction detection was keyed on `subject`, which doubled as the routing
  category. Real geo-audit output has many findings per category, so all four
  `ai_visibility` findings were rejected as contradictory and the first GBS Africa
  run returned no route. Split into `claim_key` (proposition) and `subject`
  (category). Two regression tests added.
- Route id mismatch: internal-gtm used `digital-platforms`, the engine and its live
  run artifacts use `digital-platforms-brand`. Aligned to the established id.

## Structural finding

The canonical capability registry was generated 2026-07-25 and records 74 skills
with 3 Codex installations. The live estate is 74 Claude and 63 Codex skills. Every
skill authored 2026-08-01, including `bridgeworks-lead-qualifier` and
`bridgeworks-prospect-router`, is invisible to it. That gap is why a second registry
was written inside the router skill. Regenerating it is Phase 3 step 1.

## Verification

61 internal-gtm tests, 10 skill-governance tests, 3 acquisition-engine tests, all
pass. `verify_map.py` confirms 34 components exist and every top-level engine path
is claimed. `prospect_research_queue.py status` runs unchanged.

## Not done

No file moved. No schedule changed. No outreach. No CRM write. No deletion. The only
changes inside the engine are 4 comment markers and 1 markdown banner.

## Next

Phase 3 step 1: regenerate the canonical capability registry. Everything else is
blocked behind it.

---

# Phase 3 (same day) — Authorized implementation

Steps 1 to 9 implemented and validated. Steps 10 and 11 blocked by design.
126 tests pass: 113 internal-GTM, 10 skill-governance, 3 acquisition-engine.

Full report: `operations/internal-gtm/migration/phase-3/PHASE-3-COMPLETION-REPORT.md`

## Headline results

- Registry regenerated: 74 -> 103 capabilities, Codex installations 3 -> 63.
  Zero governance concerns. 24 new capabilities enter as `unknown`, not approved.
- 32 skills have two live versions, Claude and Codex copies with different content.
  Recorded on each record as `runtime_divergence`, not only in a report.
- Duplicate route registry, qualification rubric and capability registry all now
  delegate to the canonical sources. Unique logic ported first.
- Routing bias corrected. Six commercial dimensions decide; capability maturity is
  reported as confidence and never selects. A true tie escalates instead of
  defaulting to GEO.
- `claim_key` invariant made system-wide. Implicit key is now `(subject, source)`,
  never `subject` alone. Seven regression tests across GEO and non-GEO.
- Telemetry schema plus store and comparison tool. Null outcome means unknown,
  never no. Zero denominators give null rates.
- Prospect reconciliation across six stores: 303 rows, 122 unique entities,
  94 in more than one store. All three real Gmail sends are orphaned from their
  prospect records by a company-key versus domain-key split.
- Shadow day 1 of 10 recorded. New selector chose `reconcile_unsafe_state`; the
  old job had no eligible rows.

## Open decisions for Emmanuel

1. Canonical qualification rubric. The versioned translation contract is built.
2. The 32 two-live-version skills: converge or split.
3. The 24 unknown-status capabilities: intake review.
4. Capacity numbers in `engine_snapshot.CAPACITY`.

## Not done

No send, no publish, no delete, no shutdown, no credential change. The 09:10 cron
is unchanged. Nine more shadow days before cutover is even eligible.

---

# Qualification decision (same day) — qualification-v1 implemented

151 tests pass: 138 internal-GTM, 10 skill-governance, 3 acquisition-engine.

## Implemented

- `qualification-v1` is canonical and owned by `operations/internal-gtm/`.
  Six dimensions, five tiers, evidence attribution per component, hard gates
  returned separately from the score.
- One implementation: the arithmetic and gates are in `gtm_core.score_qualification`.
  `qualification_v1.py` derives provenance, freshness and confidence from the
  evidence ledger and delegates, so a caller cannot assert freshness it lacks.
- Separation of concerns enforced in code. `FORBIDDEN_CONTEXT` refuses route id,
  service route, diagnostic score, diagnostic capability, geo score and gap
  categories. Service-route existence is a gate, not a score.
- Both slices, and telemetry, now carry the six component scores, the tier and the
  actionability state.
- Historical rubrics are compatibility readers. `qualification_contract.translate`
  returns `canonical_qualification: None` and `requires_qualification_v1_rescore`.

## The finding that matters

Both live prospects lose 30 points to the same two unscored components:

| Prospect | Old rubric | qualification-v1 | Actionability |
|---|---|---|---|
| Premier FM | 94 HOT | 50 WATCH | actionable |
| GBS Africa | 77 HOT | 59 QUALIFIED | blocked_contradicted_evidence |

`commercial_fit` 0/20 and `execution_readiness` 0/10 on every record. The whole
pipeline has been qualified on website evidence alone. The old rubric could not see
it because it had no dimension for it.

## Pre-cutover work

1. Reconciliation accounting receipt written. 303 vs 339 rows, 122 vs 120 entities,
   8 vs 12 contacted, 3 vs 0 orphaned sends. Each difference explained by source
   scope and counting rule. Numbers not forced to match.
2. GTM-critical divergence classified. Five are intentional runtime adaptation
   (identical Codex operating-rule block). Five are canonical shared implementation.
   Seven need review, including the GEO orchestrator being Claude-only while its
   five specialists are Codex-only and ungoverned.
3. Orphaned Gmail sends resolved to zero from the live mailbox. The local log was a
   six-fold undercount: 19 sent messages across 10 businesses, not 3 across 3.
4. Shadow day 1 of 10 recorded. New selector chose `reconcile_unsafe_state`.

## Not done

No send, publish, delete, shutdown or credential change. 09:10 cron unchanged.

---

# Pre-cutover completion (same day)

175 tests pass: 162 internal-GTM, 10 skill-governance, 3 acquisition-engine.
Report: `operations/internal-gtm/migration/phase-3/PRE-CUTOVER-COMPLETION-REPORT.md`

1. `geo-audit` installed in Codex as a derived runtime adaptation. Eight parity
   checks pass. Codex GTM automation no longer depends on Claude Code at runtime.
   Contract-verified, not execution-verified: no Codex session was run.
2. Five GEO specialists passed all eight governance requirements and were promoted
   to active, each with an intake record. Registry: 84 active, 19 unknown.
3. RevOps resolved with a shared bounded capability,
   `bridgeworks-gtm-pipeline-health`, in both runtimes. Six roles repointed,
   data-steward's reference removed as scope creep. `revops` stays for client work.
4. Qualification coverage added as a separate measure. `zero_because` distinguishes
   missing research from a verified negative. Coverage never touches the score.
5. Commercial Intelligence role and capability. Free tools first. Never fabricates
   a contract value: a band needs published arithmetic or it is null.
6. Buyer Intelligence role and capability. Five buyer roles with per-role
   attribution confidence. Never contacts anyone, never picks the service route.
7. Enrichment loop wired. One dimension-to-capability map shared with the
   scheduler. Covered dimensions are never re-researched.
8. Canonical contacted-company definition in one module: 10 companies, 19 sent
   messages, 0 ambiguous, 0 unmatched, all by canonical domain match.
9. Shadow day 1 of 10 re-run. Qualified untouched fell 13 -> 1 because provisional
   tiers are gone and 46 of 48 prospects are not yet scored under qualification-v1.

Headline: commercial_fit and execution_readiness are missing on 100 percent of
everything scored. Mean coverage 58.4 percent. Zero fully covered prospects.

09:10 cron unchanged. No outreach, no CRM mutation, no shutdown, no credential change.

---

# Shadow objective refinement (same day)

198 tests pass: 185 internal-GTM, 10 skill-governance, 3 acquisition-engine.
Report: `operations/internal-gtm/migration/phase-3/SHADOW-REFINEMENT-REPORT.md`

- Unsafe severity split into `unsafe_active` and `unsafe_contained`. GBS Africa
  reclassified as contained: stage cannot reach an external action, no approval
  exists, isolated. It moves from priority class 1 to class 4.
- Seven-class canonical hierarchy implemented. Within-class ranking uses expected
  commercial value, urgency, evidence confidence and effort. Maturity excluded and
  a test asserts no factor name contains it.
- Day 1: `complete_qualification_coverage` selected, rank 3 of 7, over creating
  supply. 48 candidates.
- Enrichment priority ranks 48 prospects 100 down to 0; 34 carry blockers. Cohort
  of 8 selected, every score explained line by line.
- Progressive spend. The first build let all 8 reach Stage 4 on Stage 1 evidence.
  Gate tightened to require no unresearched dimension. Now 8 -> 1 -> 1 -> 1.
- First real batch produced. Real Stage 2 research on Premier FM from free public
  sources: legal entity Otthonbiztonsagban.hu Kft, tax number, KPI counters with
  no values, two named staff, no published headcount or client count.
  `commercial_value_band: null` with the reason recorded. Budget owner and decision
  maker left null. One approval packet, digest 8820ab98, not sent.
- BV Integrated Ltd: Companies House 16768029, incorporated 2025-10-07, registered
  at a service address. Highest priority in the cohort. A commercial-fit question
  to answer before any diagnostic spend.
- Shadow log now records the priority class and why higher classes were not
  selected. Earlier rows stay comparable.

09:10 cron unchanged. No outreach, no CRM mutation, no cutover.

---

# Canonical state reconciliation (same day)

228 tests pass: 215 internal-GTM, 10 skill-governance, 3 acquisition-engine.
Report: `operations/internal-gtm/migration/phase-3/CANONICAL-STATE-RECONCILIATION.md`

- Premier FM canonical state is `diagnostic_selected`, diagnostic `not_run`. The
  diagnostic block carried run_id `acquisition-engine:...`, an adapter reshape of
  existing research, not a client-audit execution. Both packets superseded and
  non-executable. Codex was right; my earlier report was wrong.
- Tilz reclassified `resolve_blocking_evidence`. `advance_active_revenue` now needs
  a dated, sourced commercial event. The coverage class also excludes blocked
  prospects, because enrichment cannot help while a block holds.
- Transition receipts implemented with all 13 fields including an evidence hash.
  premier-fm 4 receipts, gbs-africa 5. GBS Africa is further along than Premier FM.
- Prerequisites enforced in code. `transitions.receipt` raises rather than skipping.
  `gtm_core.advance` is strictly sequential. Pre-gate artifacts are marked
  superseded and non-executable, never deleted.
- `exhausted_unknown` added. Scores zero like `required`; the scheduler will not
  reopen it. Advancement thresholds unchanged, pinned by a test.
- One canonical batch receipt, contract `gtm-batch-receipt-1`, 8 prospects,
  complete. Zero canonical packets, zero completed diagnostics, all at `qualified`.
- Stage 2 resumed on Arc Solutions Limited (priority 82). Founded 2009, 20+ clients,
  500+ projects, 9 locations, 7 service lines, no registration number published.
  Value band null: counts without prices cannot be multiplied. All five buyer roles
  null, /about is a 404, buyer_accessibility recommended `exhausted_unknown`.

09:10 cron unchanged. No outreach, no CRM mutation, no cutover, nothing deleted.

---

# Writer ownership and Arc requalification (same day)

246 tests pass: 233 internal-GTM, 10 skill-governance, 3 acquisition-engine.

## Control-plane lock

`scripts/control_lock.py`, state in `control-plane/locks.json`. No service, no
database, one repo-local JSON file.

Standing ownership: `claude_code` owns implementation (scripts, adapters, tests,
schemas, registries, roles, runtime adaptations, handoffs); `codex` owns operating
state (runs/, migration state, receipts, shadow, scheduler). A runtime meeting a
conflicting active lock fails closed. 18 tests cover conflict, expiry, same-owner
continuation, release, and both ownership directions.

Verified live: with the implementation lock held, a write to
`runs/transition-receipts/` was refused with "operating_state belongs to codex".

## The lock proved itself immediately

`runs/intelligence/arc-solutions-limited-2026-08-11.json` was found corrupt,
truncated mid-object at line 143, during this session. A concurrent write damaged
it. The reader had been swallowing the parse error and silently scoring without it.

Fixed: an unreadable listed evidence source now raises rather than being skipped.
The file lives in Codex's lane so it was not repaired here; it is withdrawn from the
source list with a recorded reason and referred to Codex. Its findings are a subset
of the stage-2 receipt, so no evidence was lost.

## Arc Solutions Limited requalification

66/100 QUALIFIED, coverage 100 percent, actionable, no exhausted dimensions.

| Dimension | Score | Status |
|---|---:|---|
| problem_evidence | 12/25 | complete, high confidence |
| commercial_fit | 16/20 | complete, medium |
| buyer_accessibility | 6/15 | complete, medium |
| trigger_urgency | 9/15 | complete, medium |
| proof_fit | 15/15 | complete, high |
| execution_readiness | 8/10 | complete, medium |

Evidence: 3 verified, 5 observed, 0 inferred, 0 stale, 0 contradicted, 0 unknown.
Self-reported scale preserved as `observed`, never promoted to `verified`. Nothing
invented: no contract value, buyer identity, signing authority or registration
number.

The semantic corrections did the work. `commercial_fit` moved 0 to 16 and
`execution_readiness` 0 to 8 with no new research, purely because absence of a price
is not absence of commercial fit, and execution readiness is not budget knowledge.

Route `digital-platforms-brand` via `client-audit`. Lifecycle stays at `routed`.
Next action: run the registered client-audit to completion, then persist the
`diagnostic_complete` receipt. No packet, no advancement.

Result is a handoff at `handoffs/arc-solutions-limited-requalification-2026-08-11.json`.
Persisting it as canonical state is Codex's lane. The canonical eight-prospect
receipt remains the cohort baseline; no competing batch was created.

09:10 cron unchanged. No outreach, no CRM mutation, no cutover, nothing deleted.
