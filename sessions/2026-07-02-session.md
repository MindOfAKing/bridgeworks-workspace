# Session 2026-07-02 — Oliviks work order (shop, CMS, award, ops)

Ran on Fable 5 then Opus 4.8 (model switched mid-session). Continued from the
2026-07-01 rebuild. Emmanuel gave a 10-point work order.

## Done and verified

1. **Committed + pushed** the full rebuild, deep links, award, session log
   (commit 492bf64), then the seed + markup note (commit 7d3d6e2). Confirmed no
   secrets staged (.env.local and .vercel are gitignored).

2. **Markup note** added to the menu page: online/delivery prices include a small
   markup over the in-store menu. Emmanuel clarified the markup is the cost of doing
   business with Wolt etc., so the site shows in-store prices with this note rather
   than reconciling the two. Deployed + verified live.

3. **Shop brand CSS applied in WordPress** (browser control, logged in as bridgeworks).
   Appended a dated brand block to the Rishi `custom_css[rishi]` customizer setting
   via the wp.customize JS API and published. Verified the marker renders in the live
   homepage's inline wp-custom-css block. Full CSS: execution/shop-brand-css/.
   NOTE: the "Jellof" product title typo fix did NOT complete — the Chrome extension
   dropped right after the CSS publish.

4. **Supabase menu CMS live** (task 6, the /admin buildout). Used the existing
   bridgeworks-ops project (jqkirludktoidwsrermq) the .env keys point to. Restored it
   from paused, applied schema + RLS, REGENERATED the seed from the approved menu.ts
   (the committed seed was stale: 5,000 soups, Banga, Abacha), seeded 33 items, set 5
   env vars locally + on Vercel, redeployed. Verified: /api/admin/menu source=supabase,
   /menu still shows approved data, admin write path 200 with secret / 401 without.

5. **Memory + session records** (task 10, "learn and master for later"): wrote 4 new
   memories (oliviks-supabase-cms, wp-shop-admin-access, vercel-deploy-workflow,
   playwright-capture-workflow), updated open-blockers + index.

## Blocked — Chrome extension disconnected mid-session
These need the extension back (Chrome foregrounded, extension signed in):
- WP: fix "Jellof" -> "Jollof" product title (task 3 remainder)
- WP: add the 15 missing dishes as WooCommerce products, then map slugs in
  shopLinks.ts (task 4)
- GBP: reply to Lili's review + publish Restaurant Guru award post (task 5). No
  Facebook — not Emmanuel's prerogative.

## Key decisions / findings
- Markup on shop prices is intentional (platform cost), not an error to fix.
- Domain flip (task 3 top-level) is gated on Aese + Cynthia being satisfied with the
  Vercel site — do not flip until they sign off.
- Oliviks CMS shares the bridgeworks-ops Supabase DB (alongside inbound_leads,
  ops_state). Flagged for a possible dedicated-project split before handover.
- Task 8: "treat the entire 3 work that needs be done" — interpreted as the 3-part
  Foundation scope (website + GBP + email/WhatsApp). Website is essentially done;
  GBP is a browser task pending; email/WhatsApp infra not yet started. Confirm intent
  next session.

## Supabase split (same session, after Emmanuel's instruction)
Oliviks now has its own dedicated Supabase project: **oliviks-kitchen**
(ref toltnysainnkpxfxibff, eu-west-1, free tier $0/month). Schema + RLS applied,
33 items seeded, Vercel env repointed, redeployed. Menu tables DROPPED from
bridgeworks-ops, which is back to only inbound_leads + ops_state. Verified:
production serves source=supabase with 33 items after the old tables were gone,
proving it reads the new project.

OPEN: SUPABASE_SERVICE_ROLE_KEY for the new project. The MCP cannot expose secret
keys and the browser was down. Copy it from
supabase.com/dashboard/project/toltnysainnkpxfxibff/settings/api-keys into Vercel
production env + website/.env.local, redeploy. Until then admin writes 503 cleanly;
public menu unaffected.

## Supabase project note
Free-tier projects pause after ~7 days inactivity. If /menu shows static fallback,
restore the oliviks-kitchen project (MCP restore_project) and it resumes.

## Next actions
1. Reconnect Chrome; finish WP typo + 15 products + GBP.
2. Confirm what "the entire 3 work" (task 8) means — likely the Foundation 3-part scope.
3. Aese: confirm shop prices, then AutoSSL fix, then plan the flip.
